# validate-install Tests

Single test wrapper to run `system/validateInstall.ps1` using the `tests/Test-code.ps1` runner.

Run:

```powershell
& 'e:\sc\git\PsWebHost\tests\Test-code.ps1' -Name 'validate-install' -Description 'ValidateInstall wrapper' -Path 'e:\sc\git\PsWebHost\tests\validate-install\test_validate-install.ps1'
```
