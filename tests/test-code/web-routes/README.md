# Web Routes Tests

This folder contains `test_web-routes.ps1` which is a minimal wrapper to check the web host responds.

Run using the test runner:

```powershell
& 'e:\sc\git\PsWebHost\tests\Test-code.ps1' -Name 'web-routes' -Description 'Web host smoke tests' -Path 'e:\sc\git\PsWebHost\tests\web-routes\test_web-routes.ps1'
```
