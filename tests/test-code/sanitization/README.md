# Sanitization Tests

This folder contains a single test script `test_sanitization.ps1` intended to be executed by the `tests/Test-code.ps1` runner.

Run:

```powershell
& 'e:\sc\git\PsWebHost\tests\Test-code.ps1' -Name 'sanitization' -Description 'Sanitization tests' -Path 'e:\sc\git\PsWebHost\tests\sanitization\test_sanitization.ps1'
```
